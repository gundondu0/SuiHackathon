import { HStack, Flex } from "@chakra-ui/react";
import React from "react";

const Header: React.FC = () => {
  return (
    <Flex justifyContent="space-between" alignItems="center" bgColor="blue.100">
      <HStack spacing="2rem">
      </HStack>
    </Flex>
  );
};

export default Header;
